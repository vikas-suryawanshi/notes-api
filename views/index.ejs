<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All notes</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>All notes</h1>
    <form method="get" action="/notes/new"><button>Create New</button></form>
    <br/>
    <%for(let note of notes){%>
        <div class="note">
            <h3>author:<%= note.author%></h3>
            <p>title:<b><%= note.title%></b></p>
            <div class="content">
                content:<%=note.content%>
            </div>
            <br/>
            <p>Time: <%= note.created_at.toString().split(" ")[4]%></p>
            <p>Date: <%= note.created_at.toString().split(" ").slice(0.3).join("-")%></p>
            <form method="get" action="/notes/<%=note._id%>/edit"><button>Edit</button></form>
            <form method="get" action="/notes/<%=note._id%>"><button>Delete</button></form>
            </div>
        </div>
        <br/>
        <hr/>
    <%}%>
</body>
</html>