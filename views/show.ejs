<% layout("/layouts/boilerplate.ejs") %>
    <div class="show">
        <h2 class="label">Author: <b><%= note.author%></b></h2>
        <p class="label">Title:<b> <%= note.title%></b></p>
        <div class="show-description">
            <%= note.content %>
        </div>
        <form method="get" action="/notes">
            <button type="submit">Home</button>
        </form>
    </div>